set nocompatible

set expandtab					" puts in spaces instead of a tab
set tabstop=4					" the numbe of spaces to use
						" use :retab to convert tabs to spaces in existing code
set shiftwidth=4				" number of spaces inserted for indentation

set softtabstop=4

set autoindent
set smarttab

set number
set ruler
